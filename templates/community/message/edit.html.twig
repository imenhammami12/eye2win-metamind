{% extends 'base.html.twig' %}
{% block body %}
    <div class="container mt-4">
        <h2>Modifier message</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-danger mt-3">Sauver</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
