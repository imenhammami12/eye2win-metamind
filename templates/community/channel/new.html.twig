{% extends 'base.html.twig' %}
{% block body %}
    <div class="container mt-4">
        <h2>Créer un Channel</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-danger mt-3">Create</button>
        <a href="{{ path('community_channels_index') }}" class="btn btn-secondary mt-3">Cancel</a>

        {{ form_end(form) }}
        <p class="mt-3 text-muted">✅ Le channel sera en attente de validation admin.</p>
    </div>
{% endblock %}
