{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin - EyeTwin{% endblock %}

{% block body %}
<section class="breadcrumb breadcrumb_bg">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb_iner text-center">
                    <div class="breadcrumb_iner_item">
                        <h2>Dashboard Admin</h2>
                        <p><a href="{{ path('app_home') }}">Accueil</a> <span>-</span> Dashboard</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<section class="blog_part section_padding">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
<<<<<<< HEAD
                    <div class="card-body text-center p-5">
                        <i class="fas fa-user-circle fa-5x text-primary mb-4"></i>
                        <h1>Bienvenue, {{ user.username }} !</h1>
                        <p class="lead">Email : {{ user.email }}</p>
                        <p>Membre depuis le {{ user.createdAt|date('d/m/Y') }}</p>
                        
                        <hr class="my-4">
                        
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle"></i> 
                            Votre compte est actif et fonctionnel !
                        </div>
                        
                        <div class="row mt-4">
                            <div class="col-md-6 mb-3">
                                <a href="{{ path('app_planning_index') }}" class="btn btn-primary w-100">
                                    <i class="bi bi-calendar-event me-2"></i>View Planning
                                </a>
                            </div>
                            <div class="col-md-6 mb-3">
                                <a href="{{ path('app_my_sessions') }}" class="btn btn-success w-100">
                                    <i class="bi bi-list-check me-2"></i>My Sessions
                                </a>
                            </div>
                        </div>

                        <a href="{{ path('app_logout') }}" class="btn btn-danger btn-lg mt-3">
                            <i class="fas fa-sign-out-alt"></i> Se déconnecter
                        </a>
=======
                    <div class="card-body">
                        <h3 class="mb-4">Vidéos uploadées</h3>

                        {% if videos is empty %}
                            <div class="alert alert-info">Aucune vidéo disponible.</div>
                        {% else %}
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Titre</th>
                                            <th>Uploaded by</th>
                                            <th>Status</th>
                                            <th>CreatedAt</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for video in videos %}
                                            <tr>
                                                <td>{{ video.title }}</td>
                                                <td>{{ video.uploadedBy ? video.uploadedBy.username : '—' }}</td>
                                                <td>{{ video.status }}</td>
                                                <td>{{ video.createdAt ? video.createdAt|date('d/m/Y H:i') : '—' }}</td>
                                                <td>
                                                    <a href="{{ path('app_video_show', { id: video.id }) }}" class="btn btn-sm btn-outline-primary">Voir</a>
                                                    <a href="{{ path('app_admin_video_edit', { id: video.id }) }}" class="btn btn-sm btn-outline-secondary">Éditer</a>
                                                    <form method="post" action="{{ path('app_admin_video_delete', { id: video.id }) }}" style="display:inline-block" onsubmit="return confirm('Supprimer cette vidéo ?');">
                                                        <input type="hidden" name="_token" value="{{ csrf_token('delete_video_' ~ video.id) }}">
                                                        <button class="btn btn-sm btn-outline-danger">Supprimer</button>
                                                    </form>
                                                </td>
                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        {% endif %}
>>>>>>> computer-vision
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}